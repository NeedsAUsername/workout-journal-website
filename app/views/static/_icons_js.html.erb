
function barbellLogoMouseOver(element) {
  element.src = '<%= asset_path('barbell-weightlifting.png') %>';
}

function barbellLogoMouseLeave(element) {
  element.src = '<%= asset_path('barbell-hold.png') %>';
}
